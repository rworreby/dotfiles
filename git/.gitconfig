[user]
	email = robin@worreby.ch
	name = Robin Worreby
[core]
	editor = atom --wait
[diff]
	tool = atom
	guitool = atom
[merge]
	tool = atom
[alias]
	logtree = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(bold yellow)%d%C(reset)' --all
